# Game Experimentation Lab

Requires the `share` directory from the LunarG Vulkan SDK symlinked. `lib` and
`include` are from Homebrew (`/opt/homebrew` or `/usr/local` depending on where
your homebrew prefix is located).
